# Dependencies used by src/redir/eval/mtagentrisk.
#
# OpenHands and MCPMark are installed from the pinned source snapshots in
# third_party/, as documented in the top-level README.
fastmcp==2.6.1
mcp==1.9.2
psycopg2-binary==2.9.12
pydantic-settings==2.9.1
tomli; python_version < "3.11"
